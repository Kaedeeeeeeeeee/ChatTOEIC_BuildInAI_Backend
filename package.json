{
  "name": "chattoeic-repo",
  "version": "1.0.0",
  "description": "ChatTOEIC Repository Root",
  "scripts": {
    "build": "cd backend/backend && npm install && npm run build",
    "start": "cd backend/backend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}