{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx prisma generate --schema=prisma/schema.prisma && npm run build"
  },
  "deploy": {
    "startCommand": "node fix-migration.js; npx prisma migrate deploy --schema=prisma/schema.prisma || echo 'Migration deploy failed, but continuing...'; node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}